{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "display": {
            "title": "display",
            "description": "output video to HDMI display",
            "type": "boolean",
            "default": false
        },
        "threshold": {
            "title": "probability threshold",
            "description": "notify when over this threshold",
            "type": "number",
            "default": 0.9,
            "minimum": 0,
            "maximum": 1
        },
        "camera_timeout": {
            "title": "camera timeout",
            "description": "camera timeout in seconds",
            "type": "number",
            "default": 1.0,
            "minimum": 0
        },
        "use_usb_camera": {
            "title": "use USB camera",
            "description": "use USB camera instead of Raspberry Pi Camera",
            "type": "boolean",
            "default": false
        },
        "shutter_time": {
            "title": "shutter time",
            "description": "shutter time (negative value means auto)",
            "type": "number",
            "default": -1
        },
        "analogue_gain": {
            "title": "analogue gain",
            "description": "analogue gain (negative value means auto)",
            "type": "number",
            "default": -1
        }
    },
    "required": [
        "display",
        "threshold"
    ],
    "propertyOrder": [
        "display",
        "threshold",
        "use_usb_camera",
        "camera_timeout",
        "shutter_time",
        "analogue_gain"
    ]
}
